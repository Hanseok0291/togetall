# Optional: canonical site URL for metadata (Open Graph, canonical link).
# On Vercel, VERCEL_URL is set automatically; set this when using a custom domain.
# Example: https://your-domain.com
# NEXT_PUBLIC_SITE_URL=
