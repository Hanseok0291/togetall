# Treadmill Pace (참조 소스)

Togetall에 통합하기 위해 이 레포에 복사해 둔 **참고용** 소스입니다.

- **정식 버전**은 별도 Git 원격에서 관리합니다. 이 폴더에는 중첩 `.git`·GitHub Actions를 두지 않습니다.
- 개발·배포는 원본 저장소 기준으로 하면 됩니다.

통합 후에는 `web/` 아래로 옮기고, 이 디렉터리는 삭제해도 됩니다.
